#include <iostream>

#include "lsp.h"

int main() {
    // Phase 1 skeleton: transport and request routing will be connected here.
    // Keep executable alive and deterministic for now.
    (void)std::cin;
    return 0;
}
